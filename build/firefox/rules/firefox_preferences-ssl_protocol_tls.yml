conflicts: []
definition_location: /home/chase/_work/complianceascode-content/complianceascode-content/products/firefox/guide/firefox/firefox_preferences-ssl_protocol_tls/rule.yml
description: To enable TLS, set <tt>security.tls.version.min</tt> to <tt>2</tt> and
  set <tt>security.tls.version.max</tt> to <tt>4</tt>.
identifiers: {}
inherited_platforms: []
ocil: 'To verify that TLS is enabled, run the following commands:

  <pre>$ grep ''security.tls.version.min'' <i>FIREFOX_INSTALL_DIR</i>/*.cfg</pre>

  The output should return:

  <pre>lockPref("security.tls.version.min", "2");</pre>

  <pre>$ grep ''security.tls.version.max'' <i>FIREFOX_INSTALL_DIR</i>/*.cfg</pre>

  The output should return:

  <pre>lockPref("security.tls.version.max", "4");</pre>'
ocil_clause: it is not enabled
oval_external_content: null
platform: null
platforms: !!set {}
prodtype: all
rationale: 'Earlier versions of SSL have known security vulnerabilities and are not

  authorized for use in DOD environments.'
references:
  disa: CCI-002450
  nist: SC-13
  stigid: DTBF030
requires: []
severity: medium
template: null
title: Enable TLS Usage in Firefox
warnings: []
