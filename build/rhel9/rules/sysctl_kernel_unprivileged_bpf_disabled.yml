conflicts: []
definition_location: /home/chase/_work/complianceascode-content/complianceascode-content/linux_os/guide/system/permissions/restrictions/sysctl_kernel_unprivileged_bpf_disabled/rule.yml
description: 'To set the runtime status of the <code>kernel.unprivileged_bpf_disabled</code>
  kernel parameter, run the following command: <pre>$ sudo sysctl -w kernel.unprivileged_bpf_disabled=1</pre>

  To make sure that the setting is persistent, add the following line to a file in
  the directory <tt>/etc/sysctl.d</tt>: <pre>kernel.unprivileged_bpf_disabled = 1</pre>'
identifiers:
  cce: CCE-83957-1
inherited_platforms: []
ocil: "The runtime status of the <code>kernel.unprivileged_bpf_disabled</code> kernel\
  \ parameter can be queried\nby running the following command:\n<pre>$ sysctl kernel.unprivileged_bpf_disabled</pre>\n\
  The output of the command should indicate a value of <code>1</code>.\nThe preferable\
  \ way how to assure the runtime compliance is to have\ncorrect persistent configuration,\
  \ and rebooting the system.\n\nThe persistent kernel parameter configuration is\
  \ performed by specifying the appropriate\nassignment in any file located in the\
  \ <pre>/etc/sysctl.d</pre> directory.\nVerify that there is not any existing incorrect\
  \ configuration by executing the following command:\n<pre>$ grep -r '^\\s*kernel.unprivileged_bpf_disabled\\\
  s*=' /etc/sysctl.conf /etc/sysctl.d</pre>\nIf any assignments other than\n<pre>kernel.unprivileged_bpf_disabled\
  \ = 1</pre>\nare found, or the correct assignment is duplicated, remove those offending\
  \ lines from respective files,\nand make sure that exactly one file in \n<code>/etc/sysctl.d</code>\
  \ contains <code>kernel.unprivileged_bpf_disabled = 1</code>, and that one assignment\n\
  is returned when \n<pre>$ grep -r kernel.unprivileged_bpf_disabled /etc/sysctl.conf\
  \ /etc/sysctl.d</pre>\nis executed.\n"
ocil_clause: the correct value is not returned
oval_external_content: null
platform: machine
platforms: !!set
  machine: null
prodtype: fedora,ol8,rhcos4,rhel8,rhel9
rationale: 'Loading and accessing the packet filters programs and maps using the bpf()

  syscall has the potential of revealing sensitive information about the kernel state.'
references:
  disa: CCI-000366
  ospp: FMT_SMF_EXT.1
  srg: SRG-OS-000132-GPOS-00067,SRG-OS-000480-GPOS-00227
requires: []
severity: medium
template:
  backends: {}
  name: sysctl
  vars:
    datatype: int
    sysctlval: '1'
    sysctlvar: kernel.unprivileged_bpf_disabled
title: Disable Access to Network bpf() Syscall From Unprivileged Processes
warnings: []
