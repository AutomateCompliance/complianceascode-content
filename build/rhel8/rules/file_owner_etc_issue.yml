conflicts: []
definition_location: /home/chase/_work/complianceascode-content/complianceascode-content/linux_os/guide/system/accounts/accounts-banners/file_owner_etc_issue/rule.yml
description: '

  To properly set the owner of <code>/etc/issue</code>, run the command:

  <pre>$ sudo chown root /etc/issue </pre>'
identifiers:
  cce: CCE-83718-7
inherited_platforms: []
ocil: 'To check the ownership of <code>/etc/issue</code>,

  run the command:

  <pre>$ ls -lL /etc/issue</pre>

  If properly configured, the output should indicate the following owner:

  <code>root</code>'
ocil_clause: /etc/issue has owner root
oval_external_content: null
platform: null
platforms: !!set {}
prodtype: fedora,ol7,ol8,rhcos4,rhel7,rhel8,rhv4,sle12,sle15,ubuntu2004,wrlinux1019
rationale: 'Display of a standardized and approved use notification before granting

  access to the operating system ensures privacy and security notification

  verbiage used is consistent with applicable federal laws, Executive Orders,

  directives, policies, regulations, standards, and guidance.<br />

  Proper ownership will ensure that only root user can modify the banner.'
references:
  cis: 1.8.1.5
requires: []
severity: medium
template:
  backends: {}
  name: file_owner
  vars:
    filepath: /etc/issue
    fileuid: '0'
title: Verify ownership of System Login Banner
warnings: []
