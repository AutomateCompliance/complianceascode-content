conflicts: []
definition_location: /home/chase/_work/complianceascode-content/complianceascode-content/products/jre/guide/java/java_jre_updated/rule.yml
description: 'If the system is joined to the Red Hat Network, a Red Hat Satellite
  Server,

  or a yum server, run the following command to install updates:

  <pre>$ sudo yum update</pre>

  If the system is not configured to use one of these sources, updates (in the form
  of RPM packages)

  can be manually downloaded and installed using <tt>rpm</tt>.

  <br /><br />

  NOTE: U.S. Defense systems are required to be patched within 30 days or sooner as
  local policy

  dictates.'
identifiers: {}
inherited_platforms: []
ocil: "If the system is joined to the Red Hat Network, a Red Hat Satellite Server,\
  \ or\na yum server which provides updates, invoking the following command will\n\
  indicate if updates are available:\n<pre>$ sudo yum check-update</pre>\nIf the system\
  \ is not configured to update from one of these sources,\nrun the following command\
  \ to list when each package was last updated:\n<pre>$ rpm -qa -last</pre>\nCompare\
  \ this to Red Hat Security Advisories (RHSA) listed at\n\n    <a xmlns='http://www.w3.org/1999/xhtml'\
  \ href='https://access.redhat.com/security/updates/active/'>https://access.redhat.com/security/updates/active/</a>\n\
  to determine if the system is missing applicable updates."
ocil_clause: it is not updated
oval_external_content: null
platform: null
platforms: !!set {}
prodtype: all
rationale: 'Running an older version of the JRE can introduce security

  vulnerabilities to the system.'
references:
  disa: CCI-002605
  nist: SI-2(c)
  srg: SRG-APP-000456
  stigid: JRE8-UX-000180
requires: []
severity: low
template: null
title: Ensure Java Patches Installed
warnings: []
