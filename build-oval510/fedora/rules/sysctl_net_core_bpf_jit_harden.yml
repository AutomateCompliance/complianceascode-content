conflicts: []
definition_location: /home/chase/_work/complianceascode-content/complianceascode-content/linux_os/guide/system/permissions/restrictions/sysctl_net_core_bpf_jit_harden/rule.yml
description: 'To set the runtime status of the <code>net.core.bpf_jit_harden</code>
  kernel parameter, run the following command: <pre>$ sudo sysctl -w net.core.bpf_jit_harden=2</pre>

  To make sure that the setting is persistent, add the following line to a file in
  the directory <tt>/etc/sysctl.d</tt>: <pre>net.core.bpf_jit_harden = 2</pre>'
identifiers: {}
inherited_platforms: []
ocil: "The runtime status of the <code>net.core.bpf_jit_harden</code> kernel parameter\
  \ can be queried\nby running the following command:\n<pre>$ sysctl net.core.bpf_jit_harden</pre>\n\
  The output of the command should indicate a value of <code>2</code>.\nThe preferable\
  \ way how to assure the runtime compliance is to have\ncorrect persistent configuration,\
  \ and rebooting the system.\n\nThe persistent kernel parameter configuration is\
  \ performed by specifying the appropriate\nassignment in any file located in the\
  \ <pre>/etc/sysctl.d</pre> directory.\nVerify that there is not any existing incorrect\
  \ configuration by executing the following command:\n<pre>$ grep -r '^\\s*net.core.bpf_jit_harden\\\
  s*=' /etc/sysctl.conf /etc/sysctl.d</pre>\nIf any assignments other than\n<pre>net.core.bpf_jit_harden\
  \ = 2</pre>\nare found, or the correct assignment is duplicated, remove those offending\
  \ lines from respective files,\nand make sure that exactly one file in \n<code>/etc/sysctl.d</code>\
  \ contains <code>net.core.bpf_jit_harden = 2</code>, and that one assignment\nis\
  \ returned when \n<pre>$ grep -r net.core.bpf_jit_harden /etc/sysctl.conf /etc/sysctl.d</pre>\n\
  is executed.\n"
ocil_clause: the correct value is not returned
oval_external_content: null
platform: machine
platforms: !!set
  machine: null
prodtype: fedora,ol8,rhcos4,rhel8,rhel9
rationale: 'When hardened, the extended Berkeley Packet Filter just-in-time compiler

  will randomize any kernel addresses in the BPF programs and maps,

  and will not expose the JIT addresses in <tt>/proc/kallsyms</tt>.'
references:
  disa: CCI-000366
  nist: CM-6b
  ospp: FMT_SMF_EXT.1
  srg: SRG-OS-000480-GPOS-00227
requires: []
severity: medium
template:
  backends: {}
  name: sysctl
  vars:
    datatype: int
    sysctlval: '2'
    sysctlvar: net.core.bpf_jit_harden
title: Harden the operation of the BPF just-in-time compiler
warnings: []
