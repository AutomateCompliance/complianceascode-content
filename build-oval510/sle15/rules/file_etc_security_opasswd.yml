conflicts: []
definition_location: /home/chase/_work/complianceascode-content/complianceascode-content/linux_os/guide/system/permissions/files/permissions_important_account_files/file_etc_security_opasswd/rule.yml
description: ' To properly set the owner of <code>/etc/security/opasswd</code>, run
  the command: <pre>$ sudo chown root /etc/security/opasswd </pre>

  To properly set the group owner of <code>/etc/security/opasswd</code>, run the command:
  <pre>$ sudo chgrp root /etc/security/opasswd</pre>

  To properly set the permissions of <code>/etc/security/opasswd</code>, run the command:
  <pre>$ sudo chmod 0600 /etc/security/opasswd</pre>'
identifiers:
  cce: CCE-85572-6
inherited_platforms: []
ocil: 'To check the ownership of <code>/etc/security/opasswd</code>,

  run the command:

  <pre>$ ls -lL /etc/security/opasswd</pre>

  If properly configured, the output should indicate the following owner:

  <code>root</code>

  To check the group ownership of <code>/etc/security/opasswd</code>,

  run the command:

  <pre>$ ls -lL /etc/security/opasswd</pre>

  If properly configured, the output should indicate the following group-owner:

  <code>root</code>

  To check the permissions of <code>/etc/security/opasswd</code>,

  run the command:

  <pre>$ ls -l /etc/security/opasswd</pre>

  If properly configured, the output should indicate the following permissions:

  <code>0600</code>'
ocil_clause: /etc/security/opasswd has owner root and /etc/security/opasswd has group
  owner root and /etc/security/opasswd has unix mode 0600
oval_external_content: null
platform: null
platforms: !!set {}
prodtype: sle12,sle15
rationale: 'The <tt>/etc/security/opasswd</tt> file stores old passwords to prevent

  password reuse. Protection of this file is critical for system security.'
references:
  disa: CCI-000200
  srg: SRG-OS-000077-GPOS-00045
  stigid: SLES-15-020240
requires: []
severity: medium
template: null
title: Verify Permissions and Ownership of Old Passwords File
warnings: []
