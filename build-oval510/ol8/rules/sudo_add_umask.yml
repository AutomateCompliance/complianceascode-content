conflicts: []
definition_location: /home/chase/_work/complianceascode-content/complianceascode-content/linux_os/guide/system/software/sudo/sudo_add_umask/rule.yml
description: 'The sudo <tt>umask</tt> tag, when specified, will be added the to the
  user''s umask in the

  command environment.

  The umask should be configured by making sure that the <tt>umask=sub_var_value("var_sudo_umask")</tt>
  tag exists in

  <tt>/etc/sudoers</tt> configuration file or any sudo configuration snippets

  in <tt>/etc/sudoers.d/</tt>.'
identifiers: {}
inherited_platforms: []
ocil: 'To determine if <tt>umask</tt> has been configured for sudo with the appropriate
  value,

  run the following command:

  <pre>$ sudo grep -ri ''^Defaults.*umask=sub_var_value("var_sudo_umask")'' /etc/sudoers
  /etc/sudoers.d/</pre>

  The command should return a matching output.'
ocil_clause: umask is not set with the appropriate value for sudo
oval_external_content: null
platform: null
platforms: !!set {}
prodtype: ol7,ol8,rhel7,rhel8
rationale: 'The umask value influences the permissions assigned to files when they
  are created.

  A misconfigured umask value could result in files with excessive permissions that
  can be read or

  written to by unauthorized users.'
references:
  anssi: BP28(R58)
requires: []
severity: medium
template:
  backends: {}
  name: sudo_defaults_option
  vars:
    option: umask
    variable_name: var_sudo_umask
title: Ensure sudo umask is appropriate - sudo umask
warnings: []
